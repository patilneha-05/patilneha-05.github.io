{
  "name": "neha-portfolio",
  "version": "1.0.0",
  "description": "Neha Patil Personal Portfolio",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./output.css",
    "watch": "tailwindcss -i ./input.css -o ./output.css --watch",
    "minify": "tailwindcss -i ./input.css -o ./output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.7"
  }
}
